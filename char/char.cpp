#include<iostream>
#include<string>
using namespace std;

int main()
{
    /*
    cout << (int)'0' << endl;
    cout << (int)'A' << endl;
    cout << (int)'a' << endl;

    cout << endl;

    cout << (int)'\0' << endl;
    cout << (int)'\t' << endl;
    cout << (int)'\n' << endl;

    char c;
    for(c='A'; c<='Z'; c++) {
    cout << c << " " << int(c) << endl; // C++
    

    char c;

    while( cin >> c )
    {
        if( 'a' <= c and c <= 'z' )
        {
            cout << "down" << endl;
            break;
        }
        if( 'A' <= c and c <= 'Z' )
        {
            cout << "upper" << endl;
            break;
        }
        if( '0' <= c and c <= '9' )
        {
            cout << "number" << endl;
            break;

        }
    }
    */
    int j;
    string s="banana";
    for(j=0;j<s.length();j++)
    {
        cout<<s[j]<<endl;


    }

   









 return 0;
}